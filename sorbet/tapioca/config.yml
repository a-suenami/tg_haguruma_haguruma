gem:
  # Exclude gems that might cause issues during initialization
  exclude:
    - sorbet
    - sorbet-runtime
    - tapioca
  workers: 1
dsl:
  # Start with minimal DSL generation
  only:
    - ActiveRecordRelations
  workers: 1