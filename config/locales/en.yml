# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  activerecord:
    models:
      tenant: Tenant
    attributes:
      tenant:
        id: ID
        name: Name
        user_page_domain: User page domain
        created_at: Created at
        updated_at: Updated at
    errors:
      models:
        tenant:
          attributes:
            base:
              reserved_id_error: This ID is reserved and cannot be used
  ruler_area:
    auth0:
      already_logged_in: Already logged in
      auth_info_not_found: Could not retrieve authentication information
      account_not_registered: This account is not registered. Logged out from Auth0.
      no_ruler_access: You do not have Ruler access rights. Logged out from Auth0.
      logged_in: Successfully logged in
      authentication_failed: "Authentication failed: %{error}"
    admins:
      title: Admin Management
      list: Admin List
      new: Create New Admin
      edit: Edit Admin
      show: Admin Details
      email: Email Address
      name: Name
      actions: Actions
      created_at: Created At
      confirm_delete: Are you sure you want to delete this admin?
      created: Admin created successfully
      updated: Admin updated successfully
      destroyed: Admin deleted successfully
      not_found: Admin not found
      errors:
        email_already_exists_in_tenant: An admin with this email address already exists in this tenant
    profiles:
      edit:
        title: Edit Profile
      form:
        email: Email Address
        email_placeholder: Enter your email address
        password: Password
        password_placeholder: Leave blank to keep current password
        password_hint: Leave blank if you don't want to change your password
      errors:
        email_already_exists: This email address is already in use
  admin_area:
    auth0:
      already_logged_in: You are already logged in
      logged_in: Successfully logged in
      auth_info_not_found: Authentication information not found
      account_not_registered: This Auth0 account is not registered in the system
      no_admin_access: You do not have admin access to this tenant
      tenant_not_found: Tenant not found
      session_expired: Your session has expired. Please log in again
      authentication_failed: 'Authentication failed: %{error}'
    admins:
      created: Admin created successfully
      updated: Admin updated successfully
      destroyed: Admin deleted successfully
      not_found: Admin not found
      errors:
        email_already_exists: This email address is already in use
  helpers:
    messages:
      created: Created successfully
      updated: Updated successfully
    action:
      new: Create
      edit: Edit
      delete: Delete
      back: Back
    submit:
      create: Create
      update: Update
  time:
    formats:
      default: '%Y/%m/%d %H:%M'
      long: '%Y/%m/%d %H:%M'
      short: '%m/%d %H:%M'
  date:
    formats:
      default: '%Y/%m/%d'
