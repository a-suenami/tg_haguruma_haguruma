ja:
  activerecord:
    models:
      tenant: テナント
    attributes:
      tenant:
        id: ID
        name: テナント名
        user_page_domain: ユーザー画面ドメイン
        created_at: 作成日時
        updated_at: 更新日時
    errors:
      models:
        tenant:
          attributes:
            base:
              reserved_id_error: このIDは予約されているため使用できません
  helpers:
    messages:
      created: 登録しました
      updated: 更新しました
    action:
      new: 作成
      edit: 編集
      delete: 削除
      back: 戻る
    submit:
      create: 登録
      update: 更新
      tenant:
        create: テナントを登録
        update: テナントを更新
  time:
    formats:
      default: "%Y/%m/%d %H:%M"
      long: "%Y/%m/%d %H:%M"
      short: "%m/%d %H:%M"
  date:
    formats:
      default: "%Y/%m/%d"
