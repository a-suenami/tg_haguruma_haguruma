<ul class="uk-breadcrumb">
  <li>
    <%= link_to 'テナント', ruler_area_tenants_path %>
  </li>
  <li>
    <%= link_to @tenant.name, ruler_area_tenant_path(@tenant) %>
  </li>
  <li>
    <%= link_to 'コンテンツタイプ', ruler_area_tenant_content_types_path(@tenant) %>
  </li>
  <li>
    <span>新規作成</span>
  </li>
</ul>

<div class="am-page-header">
  <h3>コンテンツタイプ新規作成</h3>
</div>

<%= form_with model: @content_type, url: ruler_area_tenant_content_types_path(@tenant), local: true, data: { turbo: false } do |f| %>
  <%= render 'form', f: f %>
<% end %>
