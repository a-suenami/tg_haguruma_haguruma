<ul class="uk-breadcrumb">
  <li>
    <%= link_to 'テナント', ruler_area_tenants_path %>
  </li>
  <li>
    <%= link_to @tenant.name, ruler_area_tenant_path(@tenant) %>
  </li>
  <li>
    <%= link_to '認可タグ', ruler_area_tenant_authorization_tags_path(@tenant) %>
  </li>
  <li>
    <span>編集</span>
  </li>
</ul>

<div class="am-page-header">
  <h3>認可タグ編集</h3>
</div>

<%= render 'form',
    form_url: ruler_area_tenant_authorization_tag_path(@tenant, @authorization_tag.id),
    form_method: :patch,
    submit_label: '更新' %>
